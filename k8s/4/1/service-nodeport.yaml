apiVersion: v1
kind: Service
metadata:
  name: service-nodeport
  labels:
    app: multi-container
spec:
    type: NodePort
    selector:
        app: multi-container
    ports:
        - protocol: TCP
          port: 9001
          targetPort: 80
          nodePort: 30001
